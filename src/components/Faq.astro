<!-- ---
import {
  defaultLang,
  isLang,
  useTranslations,
  type Lang,
} from '../i18n/ui';

const lang: Lang = isLang(Astro.currentLocale) ? Astro.currentLocale : defaultLang;
const t = useTranslations(lang);

const faqs = [
  { q: t('faq.q1'), a: t('faq.a1') },
  { q: t('faq.q2'), a: t('faq.a2') },
  { q: t('faq.q3'), a: t('faq.a3') },
  { q: t('faq.q4'), a: t('faq.a4') },
  { q: t('faq.q5'), a: t('faq.a5') },
];

const nbspPunct = lang === 'fr' ? / ([?!:;])/g : null;
---

<section class="section" id="faq" aria-labelledby="faq-title">
  <div class="container narrow">
    <div class="section-head">
      <p class="eyebrow">{t('faq.eyebrow')}</p>
      <h2 id="faq-title">
        {t('faq.title.before')}<span class="accent">{t('faq.title.accent')}</span>
      </h2>
    </div>
    <div class="faq">
      {
        faqs.map((f) => (
          <details>
            <summary>{nbspPunct ? f.q.replace(nbspPunct, '\u00a0$1') : f.q}</summary>
            <p>{f.a}</p>
          </details>
        ))
      }
    </div>
  </div>
</section>

<style>
  .narrow {
    max-width: 60rem;
  }

  .faq {
    border-top: 1px solid var(--border-strong);
  }

  details {
    border-bottom: 1px solid var(--border-strong);
  }

  summary {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: var(--space-4);
    padding-block: var(--space-6);
    font-size: clamp(1.125rem, 1rem + 0.4vw, 1.375rem);
    font-weight: 500;
    letter-spacing: -0.02em;
    cursor: pointer;
    list-style: none;
  }

  summary::-webkit-details-marker {
    display: none;
  }

  summary::after {
    content: '';
    flex: none;
    width: 2rem;
    height: 2rem;
    border-radius: var(--radius-pill);
    background:
      linear-gradient(var(--ink), var(--ink)) center / 12px 2px no-repeat,
      linear-gradient(var(--ink), var(--ink)) center / 2px 12px no-repeat,
      var(--color-light-blue);
    transition: transform 0.2s ease;
  }

  details[open] summary::after {
    background:
      linear-gradient(var(--ink), var(--ink)) center / 12px 2px no-repeat,
      var(--color-amethyst);
  }

  details p {
    max-width: 44rem;
    padding-bottom: var(--space-6);
    color: var(--ink-2);
  }
</style>

-->
